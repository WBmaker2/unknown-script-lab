# 04. 미지의 문자 해독 연구소

- 프로젝트: `unknown-script-lab`
- 작성일: 2026-09-15 / 상태: 구현 전 설계 / 우선순위: 4
- 대상: 중·고등학교 국어·수학·정보 / 활동: 25분
- [공통 원칙](./00-shared-design-principles.md) · [목록](./README.md)

## 1. 질문과 차별성

‘사전 없이 반복되는 기호와 장면만으로 언어 규칙을 찾을 수 있을까?’
기존 낱말 뜻·지시어·문맥 복원은 알려진 언어를 해석하지만, 이 앱은 가상 언어의 어휘와 문법을 증거로 추론한다.
4순위 이유는 복잡한 수치 엔진 없이도 깊이 있는 가설 검증과 대량 이미지 탐구를 결합할 수 있기 때문이다.

## 2. 목표·오개념

- 반복과 대비로 단어·어순·복수 표지의 가설을 만든다.
- 새 문서에서 기존 가설을 검사하고 반례가 나오면 수정한다.
- 아직 구분할 수 없는 두 해석을 ‘모름’으로 남긴다.
- ‘그림에 나온 모든 정보가 문장에 들어 있다’, ‘한 예문만 맞으면 해독 완료’를 방지한다.
- 실제 고대 문자나 특정 문화의 언어를 복원하는 앱으로 소개하지 않는다.

## 3. 25분 탐구

1. 3분: 사람·물건·수량이 그려진 문서 3장의 공통 기호를 찾는다.
2. 5분: 기호-뜻 가설 3개를 연결하고 근거 문서를 지정한다.
3. 5분: 새 문서 2장 중 어떤 것이 가설을 더 잘 구분할지 선택한다.
4. 7분: 반례를 반영해 어순과 복수 표지 규칙을 고친다.
5. 5분: 보지 못한 문서 3장을 해석하고 남은 모호함을 설명한다.
- 예: ‘아이 사과 받다’와 ‘어른 사과 받다’가 한 기호만 다르고, ‘아이 사과 주다’가 다른 한 기호만 다르다.
- 전이: 대상 그림은 유지한 채 주는 사람과 받는 사람이 바뀐 장면을 해독한다.

## 4. 화면·상태

| 화면 | 정보 | 행동 |
|---|---|---|
| 문서 보관함 | 문서·발견 순서 | 비교할 문서 고르기 |
| 단서 비교 | 같은 기호와 다른 기호 | 표시·근거 연결 |
| 가설 노트 | 기호 뜻, 어순, 확신 상태 | 가설 수정·보류 |
| 추가 조사 | 자료 비용과 후보 | 문서 열기 |
| 새 문서 시험 | 미공개 예문 | 번역·모호함 표시 |

관찰 → 가설 → 시험 → 수정 → 전이 상태를 사용한다. 이전 가설은 삭제하지 않고 이력으로 남긴다.
기호 연결은 드래그 외 선택 후 뜻 버튼 방식도 제공한다. 근거 없는 확정에는 오답 경고 대신 근거를 요청한다.
자산 누락 시 구조화된 장면 설명과 정확한 기호 문자열을 보여준다.

## 5. 인공 언어 모델

- P0 어휘 12개: 행위자 3, 대상 3, 동사 3, 수량 2, 복수 표지 1. 장면 의미 목록은 고정한다.
- 문법 3개: 기본 순서 행위자-대상-동사, 수량어 위치, 복수 표지 부착 위치.
- 장면은 actor, action, object, count의 구조화된 의미를 가진다. 이미지가 정답을 생성하지 않는다.
- 정확한 기호는 앱에서 SVG 글리프로 그린다. 비슷한 기호는 고의 난이도가 아닌 한 사용하지 않는다.
- encoder(meaning, grammar)와 parser(tokens, hypothesis)를 분리한다.
- 가능한 어휘 대응과 문법 후보의 집합 H를 관리한다. 증거가 늘면 일치하는 후보만 남긴다.
- 큰 전수 탐색 대신 교육용으로 사전 정의한 후보 20~60개를 사용한다. 후보 수는 가능한 모든 언어의 수가 아님을 표시한다.
- 미션 제작 시 제공 증거로 정답 후보를 구분할 수 있는지 검사한다. 모호함 미션은 의도적으로 복수 후보를 남긴다.
- 후보 선택의 유용성은 남은 가설을 얼마나 구분하는지로 설명하며 학생에게 엔트로피 수식은 강제하지 않는다.

## 6. 데이터·평가

- Glyph: id, svgPath, accessibleName. glyph에는 의미 라벨을 직접 노출하지 않는다.
- Meaning: actorId, actionId, objectId, count.
- Document: id, imageId, meaningId, tokens, evidenceLevel, unlockCost.
- Hypothesis: lexiconMapping, grammarId, supportingDocumentIds, status.
- Mission: initialDocuments, optionalDocuments, holdoutDocuments, expectedAmbiguities.
- 자동 판정은 의미 구조의 일치와 허용된 동의어를 사용한다. 자유 서술을 문자열 완전 일치로 채점하지 않는다.
- 가설 보류·복수 해석은 실제로 남은 후보와 일치할 때 정당한 답으로 인정한다.
- 평가 요소: 단서 연결, 반례 반영, 새 문서 적용, 불확실성 구분.

## 7. 이미지 제작 계획

- P0: 장면 18장과 종이·간판 배경 6장. 전체: 장면 60장+문서 배경 20장=80장.
- 예시: ‘가상의 항구 마을, 아이 한 명이 어른에게 사과 한 개를 건네는 장면, 두 손과 사과의 이동 관계가 분명함, 일정한 교육용 삽화 화풍, 문자 없음’.
- 고정: 등장인물 외형·물건 모양·시점. 변경: 행위자·행동·수량 중 지정한 한 요소.
- 검수: 실제 개수, 주는/받는 방향, 인물 혼동, 가려진 물건. 의미와 어긋나는 이미지는 재생성 또는 제외한다.
- 정확한 문자는 검수 후 별도 합성한다. 생성 이미지 속 무의미한 글자는 제거한다.
- Three.js는 P0에 사용하지 않는다. 책상 장식보다 문서 비교의 가독성을 우선한다.

## 8. 범위·구현 순서

- P0: 언어 하나, 기호 12개, 미션 3개, 가설 노트, 새 문서 시험.
- P1: 다른 어순 언어, 부정 표지, 고의적 모호함, 정보 선택 예산.
- 제외: 실제 언어 번역 API, 자유 생성 문법, 학생 신상 기반 장면, 역사적 사실 주장.
- M1: 의미·문법·검증 예문 → M2: 후보 판정 → M3: 텍스트만으로 학습 흐름 → M4: 이미지 검수·삽입 → M5: 모호함 검토.
- 모듈: grammar.ts, encoder.ts, hypothesisValidator.ts, missions.ts, Glyph.tsx, EvidenceBoard.tsx, HypothesisNote.tsx. 각각 500줄 미만.

## 9. 검증·완료 기준

- 모든 정답 의미에 대해 parse(encode(meaning))가 같은 의미를 반환한다.
- 초기 문서에서 후보 2개가 남고 추가 문서로 1개가 되는 검수 미션을 고정한다.
- 일부러 동일 증거를 만드는 두 후보는 한쪽만 정답 처리하지 않는다.
- 보류·되돌리기·문서 열기 취소가 조사 예산과 가설 이력을 깨뜨리지 않아야 한다.
- 이미지 24장의 의미 레코드와 실제 표시를 전수 대조한다.
- 키보드 기호 선택, 320px 문서 세로 비교, reduced-motion, 이미지 미표시 대체 설명을 검증한다.
- gi-pulse는 추가 조사/새 문서 시험 버튼에 적용하며 업데이트 내역을 제공한다. VoiceOver·음성 기능 제외.
- 완료: 미션 3개가 실제 증거로 풀리고, 의도된 모호함을 인정하며, 가설 수정 과정이 기록된다.
- 위험: 그림이 과잉 단서를 주거나 반대로 중요한 관계를 숨김. 먼저 의미·논리를 검수하고 이미지는 그 뒤에 맞춘다.
