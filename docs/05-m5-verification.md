# M5 — 모호함·접근성·반응형·성능 검증

- 모호함: 모호함 미션에서 끝까지 2+ 후보 잔류·보류 정당 판정(테스트), 시험에서 모름 제출 시 잔류 후보와 대조해 정당한 보류 인정. 동일 증거 쌍은 한쪽만 정답 처리하지 않음(테스트).
- 접근성: 전부 네이티브 버튼·셀렉트·다이얼로그(키보드 Tab 순서 미션→시작 확인, Enter 시작 확인), 포커스링 3px, 드래그 없음(선택+버튼 방식), 색외단서(핀라벨 텍스트+모양), reduced-motion 정적 테두리, VoiceOver·TTS 범위제외(명시).
- 대비(실측): 본문 14.15, 보조 8.61, 버튼백/액센트 5.27, 핀 6.52/5.11, 플레이스홀더 5.83 — 전부 4.5:1 이상.
- 반응형: 320px에서 scrollWidth==clientWidth·넘침 요소 0, 390px·데스크톱 스크린샷 확인, 2열→1열 적층.
- 성능: dist JS 248KB(gzip 79KB)·CSS 4.6KB, 엔진 순수함수·프레임 계산 없음(탭숨김 정책 해당없음, 입력 반응 즉시). 목표 100ms 내 충족( measured 아님 — 배포점검에서 명시).
- 검출기: `impeccable detect --json src index.html` → 0건.
- 다음: 배포 직전 점검.
