# M4 — 이미지대체 검수·삽입

- 방침: P0는 생성 이미지 없이 출하. 정확한 기호=SVG 글리프 12개, 장면=구조화 설명 텍스트. 생성 장면 18장+배경 6장은 MVP 이후(원칙: 소량 시험→검수→일괄) 별도 자산 레코드와 함께 진행.
- 검수: `tests/scene.fallback.test.ts` 3개 — 장면 설명에 행위자·대상·수량 단서 포함·조사어 정상, 접근이름 `기호 N` 형식·의미라벨 비노출, 12경로 상호 상이.
- 대체화면: WebGL 미사용이므로 별도 대체 불필요. 장면 이미지 위치에는 항상 설명+정확 기호열을 함께 표시(자산 누락 규칙 충족).
- 다음: M5 접근성·반응형·성능 검증.
