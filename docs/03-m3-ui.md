# M3 — 텍스트 학습흐름 UI

- 결과: 5화면(보관함/비교/가설노트/추가조사/시험) + 미션 선택·단계탭·연구노트 요약·업데이트내역·JSON내보내기. `store.ts` (무PII·세션폴백·내보내기), `Glyph.tsx` (SVG·의미라벨 비노출), `DocCard` (장면설명+기호열+핀라벨), `EvidenceBoard` (동일/차이 기호+선택후 뜻버튼 연결, 근거없음→요청), `HypothesisNote` (후보수·보류정당·이력, 삭제없음), `InvestigateView` (비용·구분수·확인/취소, 취소 무손실), `TrialView` (4선택+모름표시, 구조판정).
- 규칙 준수: 첫화면 1질문+1시작버튼, gi-pulse 단일(다음 제안 탭 1개), 2열→1열 적층, 조사어(아가/를·을) 수정, `tsc`·13테스트·`vite build` 통과.
- 검증(1차): 데스크톱·390px 스크린샷 — 오버플로 없음, 후보 2/27 표시, 예산·연결 카운터 정상.
- 다음: M4 이미지대체 검수, M5 접근성·반응형·성능.
