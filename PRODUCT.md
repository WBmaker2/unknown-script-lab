# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Stack

delegated: TypeScript 기반 정적 웹앱을 기본 후보로 하고 상세 선정·버전 고정은 구현 시 공식 문서 호환성 확인 후 결정 (사용자가 에이전트에 위임). 현 시점 스캐폴드·프레임워크 없음. 배포 타깃 미정.

## Users

- 1차: 중·고등학교 국어·수학·정보 학생. 25분 수업 상황에서 직접 문서 비교·가설 연결·새 문서 시험을 조작한다.
- 병행 사용자: 교사. 15~30분 활동 + 5분 정리 형태로 운영한다.
- 대상 학년 표기는 난이도 제안이며 공식 성취기준 매핑은 별도 검토로 남긴다.

## Product Purpose

- 사전 없이 반복되는 기호와 장면만으로 가상 언어의 어휘·어순·복수 표지 규칙을 증거로 추론하게 한다.
- 반복과 대비로 가설을 만들고, 새 문서에서 검사하고, 반례가 나오면 수정하고, 구분 불가한 해석은 ‘모름’으로 남기는 탐구 사이클을 수행한다.
- 성공: 미션 3개가 실제 증거로 풀리고, 의도된 모호함을 정당한 답으로 인정하며, 가설 수정 과정이 이력으로 기록된다.

## Positioning

- 알려진 언어의 낱말 뜻·지시어·문맥 복원이 아니라, 가상 언어의 어휘와 문법을 증거로 추론하는 가설-검증 앱이다.
- 복잡한 수치 엔진 없이도 가설 검증 논리와 대량 이미지 탐구를 결합한다 (4순위 선정 이유).
- 실제 고대 문자·특정 문화 언어 복원으로 소개하지 않는다.

## Operating Context

- 25분 흐름: 3분 공통 기호 찾기(문서 3장) → 5분 기호-뜻 가설 3개 연결+근거 지정 → 5분 가설을 잘 구분할 새 문서 선택(후보 2장 중) → 7분 반례 반영 어순·복수 규칙 수정 → 5분 미공개 문서 3장 해석+남은 모호함 설명. 전이 과제로 인물 역할이 바뀐 장면 해독.
- 학습 공통 흐름: 예측 → 조건 설정 → 실행/관찰 → 근거 기록 → 조건 변경 재검증 → 전이. 답 확인 전 간단한 예측 기록, 조건·결과·해석 분리 저장, 실행 후 바뀐 조건을 이전 결과에 덮어쓰지 않음.
- 운영 환경: 교실. 데스크톱은 시각 자료+조건 패널 병렬, 모바일은 시각 자료 → 조건 → 실행 → 결과 순 적층. 검증 폭 320·360·768·1280px.
- 확인된 기능 화면(워크플로 단계): 문서 보관함, 단서 비교, 가설 노트, 추가 조사, 새 문서 시험. 관찰 → 가설 → 시험 → 수정 → 전이 상태 사용.

## Capabilities and Constraints

- P0: 언어 1개, 기호 12개(행위자 3·대상 3·동사 3·수량 2·복수 표지 1), 미션 3개, 가설 노트, 새 문서 시험. P1(부정 표지·다른 어순·고의적 모호함·정보 선택 예산)은 P0 충족 후 확장, 한 번에 전부 구현하지 않음.
- 문법 3개: 기본 순서 행위자-대상-동사, 수량어 위치, 복수 표지 부착 위치. 장면 의미는 actor·action·object·count 구조화. 이미지가 정답을 생성하지 않음.
- encoder(meaning, grammar)와 parser(tokens, hypothesis) 분리. 후보 집합 H는 교육용 사전 정의 20~60개로 사용하며 가능한 모든 언어의 수가 아님을 표시. 증거 증가 시 일치 후보만 유지. 미션 제작 시 제공 증거로 정답 후보 구분 가능 여부 검사, 모호함 미션은 의도적 복수 후보 유지. 후보 유용성은 남은 가설 구분 정도로 설명, 엔트로피 수식 강제 없음.
- 데이터: Glyph(id, svgPath, accessibleName; 의미 라벨 노출 금지), Meaning(actorId, actionId, objectId, count), Document(id, imageId, meaningId, tokens, evidenceLevel, unlockCost), Hypothesis(lexiconMapping, grammarId, supportingDocumentIds, status), Mission(initialDocuments, optionalDocuments, holdoutDocuments, expectedAmbiguities).
- 판정: 의미 구조 일치+허용 동의어 사용, 자유 서술 문자열 완전일치 채점 금지. 가설 보류·복수 해석은 실제 남은 후보와 일치할 때 정당 답 인정. 평가 요소: 단서 연결·반례 반영·새 문서 적용·불확실성 구분. 이전 가설 삭제 금지, 이력 유지. 근거 없는 확정은 오답 경고 대신 근거 요청.
- 엔진·데이터: 계산 엔진은 렌더러 분리 순수 함수(DOM·Three.js 의존 금지), 구조 engine/·models/·scenarios/·views/·assets/·tests/, 500줄 전 기능 분리. 입력에 유효 범위·단위 명시, 범위 밖·NaN·무한대·중복 실행 차단. 난수 실험은 seed·engineVersion·scenarioVersion·입력·시행 수 저장 재현. 프레임 속도와 수치 시간 간격 분리, 탭 숨김 시 계산 중단+재개 정책 명시. 무거운 계산 Worker 검토, 취소·구 결과 무시·진행/재시도 제공. WebGL 실패 시 SVG/Canvas/표 대체(핵심 학습 유지, 미지원 기능 설명). localStorage는 비식별 실험 기록만, 불가 시 세션+JSON 내보내기. 기록 포맷: schemaVersion, appId, createdAt, scenarioId, parameters, seed, observations, prediction, explanation.
- 이미지: P0 장면 18장+종이·간판 배경 6장, 전체 80장(장면 60+배경 20). 정확한 기호는 앱 내 SVG 글리프로 렌더, 생성 이미지에 문자·수치·기호·수식·기하를 맡기지 않고 검수 후 별도 합성, 생성 이미지 내 무의미 글자 제거. 비교 세트는 고정 요소/변경 요소 구분, 비의도 단서 제외. 가상 vs 실제 관측 자료 구분 표시. MVP 자산부터 소량 시험→검수→일괄→압축→삽입→화면 검수, 한 번에 전체 다운로드 금지. WebP/AVIF 검토, 목표 카드 150KB 이하·주요 장면 500KB 이하(화질 따라 조정). Three.js P0 미사용. 유사 시도 3회 실패 시 중단·협의.
- 제외: 실제 언어 번역 API, 자유 생성 문법, 학생 신상 기반 장면, 역사적 사실 주장, 학생 개인 식별 정보 저장.
- 미결정(발명 금지): 라이브러리 버전(구현 시 고정), 공식 성취기준 매핑(별도 검토), 이미지 모델(image 2.5) 가용성·입력 제약·가격·라이선스(제작 착수 시 확인, 조용한 대체 금지), 수량·오차·성능 예산은 설계 목표(검증됨이 아님), 중복 검토는 공개 목록·로컬 폴더·앞선 주제 기준(외부·미등록 전체 배제 아님).

## Brand Commitments

- 명칭: `unknown-script-lab` / 미지의 문자 해독 연구소.
- 바인딩 제약(제시된 범위 내 기록, 확장 없음): 밝은 한국어 UI, 첫 화면 질문 1개+시작 버튼 1개, 중요 다음 행동 한 버튼만 강조하는 aura(요청 명칭 gi-pulse; reduced-motion 시 정적 테두리), 색만으로 상태 구분 금지(범례·텍스트·모양 병행), 항상 찾을 수 있는 위치의 작은 ‘업데이트 내역’ 버튼(최초 개발일·개선일·설명, 실제 변경 시 기록).

## Evidence on Hand

- `./00-shared-design-principles.md`: 12앱 공통 설계·구현 원칙 (구현 전 설계, 2026-09-15).
- `./04-unknown-script-lab.md`: 본 앱 설계 brief (구현 전 설계, 우선순위 4).
- 없음(향후 작업에서 날조 금지): 구현 코드, 생성 이미지·자산 레코드, 측정 성능·수치 오차, 수업 효과 검증, 추천사·외부 검증, 공개 URL·HVC 주소.
- 앱별 예제 값·허용오차는 구현 검증용 설계 사례이며 엔진·단위에 맞춰 재확인 필요.

## Product Principles

1. 증거 우선: 모든 대응·규칙 주장은 지지 문서와 함께 남기고, 반례가 나오면 가설을 수정한다.
2. 모호함 보존: 구분 불가한 해석을 억지 단일 정답으로 닫지 않고 ‘모름·복수 후보’로 기록한다.
3. 오개념 방지: ‘그림 정보 전부 문장에 포함’, ‘한 예문 적중=해독 완료’ 같은 단순화를 평가·피드백에서 차단한다.
4. 25분 운용 가능: 교사가 15~30분+5분 정리로 돌릴 수 있는 단계·조작·복구 범위를 넘지 않는다.
5. 재현·분리: 의미·논리·판정을 이미지·렌더러와 분리하고 seed·버전·입력으로 재현한다.

## Accessibility & Inclusion

- 필수: 모든 드래그에 숫자 입력·버튼·방향키 등 대체 조작, 키보드 포커스·다이얼로그 닫기/복귀 검증, 320px 가로 넘침 없음, 모션 축소 대응, 오류 복구 경로, 2D 대체 화면, 색 외 단서(범례·텍스트·모양) 제공.
- 범위 제외(명시): VoiceOver 구현·검증 제외, TTS·음성 녹음·자동 재생 오디오 미포함.
