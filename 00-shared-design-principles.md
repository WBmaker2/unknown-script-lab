# 공통 설계·구현 원칙

작성일: 2026-09-15. 상태: 구현 전 설계. [개발 순서](./README.md)

## 1. 범위와 역할

- 본 묶음은 12개 앱의 설계이며 현재 앱 구현·이미지 생성·배포를 뜻하지 않는다.
- Astra는 기획, 계산 모델 검토, 오개념 검토와 리뷰를 담당한다.
- 실제 구현·코딩·커밋·푸시·배포는 사용자 지침에 따라 Luna가 담당한다.
- 문서 작성 중 Luna는 크레딧 부족으로 실행되지 않았다. 기획 문서는 주 에이전트가 작성한다.
- image 2.5는 사용자 지정 이미지 모델 명칭이다. 실제 사용 가능 여부·입력 제약·가격·라이선스는 제작 착수 시 확인하며 다른 모델로 조용히 대체하지 않는다.
- 모든 수량·오차 기준·성능 예산은 설계 목표다. 측정 결과나 교육 효과가 검증되었다는 뜻이 아니다.
- 현재 공개 목록, 로컬 프로젝트 폴더와 앞선 설계 주제를 기준으로 중복을 검토했다. 모든 외부·미등록 프로젝트까지 배제한 것은 아니다.

## 2. 학습 공통 흐름

예측 → 조건 설정 → 실행/관찰 → 근거 기록 → 조건을 바꿔 재검증 → 전이 과제.

- 답을 보기 전 간단한 선택 또는 예측 기록을 남긴다. 긴 글 입력을 강제하지 않는다.
- 조건, 결과, 해석을 구분해 저장한다. 실행 이후 바뀐 조건은 이전 결과에 덮어쓰지 않는다.
- 정답 하나가 없는 과제는 근거 일관성·반례 검토·모델 한계 인식으로 평가한다.
- 실패 시 무엇이 막혔고 무엇을 바꾸면 되는지 알려준다. 임의의 성공 수치를 만들지 않는다.
- 교사는 15~30분 활동과 5분 정리로 사용할 수 있다. 문서의 대상 학년은 난이도 제안이며 공식 성취기준 매핑은 별도 검토한다.

## 3. 화면과 조작

- 밝은 한국어 UI, 첫 화면에 질문 하나와 시작 버튼 하나를 배치한다.
- 주요 화면은 미션, 실험, 비교, 기록으로 구성한다. 화면 폭 320·360·768·1280px에서 흐름을 검증한다.
- 데스크톱은 시각 자료와 조건 패널을 나란히, 모바일은 시각 자료 → 조건 → 실행 → 결과 순으로 쌓는다.
- 모든 드래그에는 숫자 입력·버튼·방향키 등 대체 조작을 제공한다. 키보드 포커스와 다이얼로그 닫기/복귀를 검증한다.
- 중요한 다음 행동에 gi-pulse 아우라를 사용한다. 한 번에 한 버튼만 강조하고 reduced-motion에서는 정적 테두리로 대체한다.
- 색만으로 상태를 구분하지 않는다. 범례, 텍스트, 모양을 함께 제공한다.
- VoiceOver 구현 및 검증은 제외한다. TTS·음성 녹음·자동 재생 오디오도 포함하지 않는다.
- 작은 ‘업데이트 내역’ 버튼을 항상 찾을 수 있는 위치에 둔다. 최초 개발일·개선일·간단한 설명을 실제 변경 때 기록한다.

## 4. 엔진과 데이터

- 계산 엔진은 렌더러와 분리된 순수 함수로 만들고 DOM·Three.js 객체에 의존하지 않는다.
- 구조 예: engine/, models/, scenarios/, views/, assets/, tests/. 파일이 500줄에 도달하기 전에 기능별로 분리한다.
- TypeScript 기반 정적 웹앱을 기본 후보로 한다. 라이브러리 버전은 구현 때 공식 문서와 호환성을 확인해 고정한다.
- 입력에는 유효 범위와 단위를 명시한다. 범위 밖 입력, NaN, 무한대, 중복 실행을 차단한다.
- 난수 실험은 seed·engineVersion·scenarioVersion·입력·시행 수를 함께 저장해 재현한다.
- 화면 프레임 속도와 수치 시간 간격을 분리한다. 탭이 숨겨졌을 때 계산을 멈추며 재개 정책을 명시한다.
- 무거운 계산은 Worker를 검토한다. 취소, 오래된 작업 결과 무시, 진행 상태와 재시도 경로를 제공한다.
- WebGL 실패 시 학습 핵심을 유지하는 SVG/Canvas/표 대체 화면을 제공한다. 대체 화면에서 지원하지 않는 기능은 설명한다.
- localStorage에는 개인 식별 정보 없이 실험 기록만 저장한다. 저장 불가 시 현재 세션과 JSON 내보내기를 제공한다.
- 기록 포맷: schemaVersion, appId, createdAt, scenarioId, parameters, seed, observations, prediction, explanation.

## 5. 생성 이미지 제작

- 이미지는 맥락·비교·가상 표본에 활용한다. 그래프, 측정값, 기호, 수식, 실제 기하 구조, 정답은 코드·검수 데이터로 생성한다.
- 제작 흐름: 구조화된 자산 목록 → 스타일 기준 이미지 → 소량 시험 → 일관성 검수 → 일괄 제작 → 압축 → 앱 삽입 → 실제 화면 검수.
- 자산 레코드: id, purpose, promptVersion, modelUsed, referenceIds, semanticLabels, alt, dimensions, licenseNote, reviewStatus.
- 비교 세트는 동일 구도·대상·조명 등 고정 요소와 변경 요소를 구분한다. 의도하지 않은 단서가 생기면 제외한다.
- 문자·수치·정확한 기호는 생성 이미지에 맡기지 않고 별도 레이어로 합성한다.
- 가상 자료와 실제 관측 자료를 구분해 표시한다. 실제 작품·문화·과학 관측처럼 오인시키지 않는다.
- 아래 앱별 수량은 전체 목표이며 MVP 자산부터 제작한다. 한 번에 전체 이미지를 내려받지 않는다.
- WebP/AVIF와 크기별 자산을 검토한다. 기본 목표는 카드당 150KB 이하, 주요 장면 500KB 이하이며 화질에 따라 조정한다.
- 생성 실패는 소수 샘플에서 원인을 검토한다. 유사 시도가 3회 실패하면 중단하고 대안을 사용자와 협의한다.

## 6. 검증과 완료 판정

1. 모델 검증: 분석 가능한 사례, 경계, 대칭, 보존량, 확률 합과 수치 오차를 확인한다.
2. 데이터 검증: 스키마, 참조, 중복, 정답과 자산의 일치, seed 재현성을 확인한다.
3. UI 검증: 실제 한 학습 흐름, 키보드, 모바일 가로 넘침, 모션 축소, 오류 복구, 2D 대체 화면을 확인한다.
4. 성능 검증: 대상 기기를 명시하고 초기 표시·조작 지연·계산 시간·메모리를 기록한다. 목표는 일반 입력 반응 100ms 내, 무거운 계산은 진행/취소 표시다.
5. 교과 검토: 단순화와 오개념을 교과 관점에서 검토한다. 테스트 통과와 수업 효과는 별개로 보고한다.
6. 배포가 승인된 후 공개 URL의 자산 경로와 기능을 직접 확인한다. 특히 저장소 하위 경로를 검증한다.
7. 배포 주소와 HVC 확인용 주소를 보고서에 클릭 가능한 링크로 제공한다. HVC 등록과 공개 갤러리 동기화는 각각 별도 범위로 취급한다.

## 7. 문서 사용 순서

- README의 순번은 개발 추천 순서이며 앱 간 선행 의존성을 뜻하지 않는다.
- 각 앱의 P0 완료 기준을 충족한 뒤 P1로 확장한다. 모든 확장을 한 번에 구현하지 않는다.
- 앱별 문서의 예제 값은 구현 검증용 설계 사례다. 허용오차는 엔진·단위에 맞춰 재확인한다.
- 외부 자료를 사용할 때 출처·열람일·버전·이용 조건을 기록한다. 이 묶음에서 링크는 참고 자료이며 전문 검증을 대신하지 않는다.
